
@{ Layout = "_Layout"; }

@section Scripts
{@RenderSection("Scripts", required: false)}

<div class="dropdown">
	<button class="btn btn-primary downdown-toggle" type="button" data-toggle="dropdown">
		Administration
		<span class="caret"></span>
	</button>
	<ul class="dropdown-menu">
		<li><a asp-action="Index">Start</a></li>
		<li><a asp-action="CreatePizza">Ny maträtt</a></li>
	</ul>
</div>

@if (ViewData["Message"] is string message && !string.IsNullOrEmpty(message))
{
	<h2>@message</h2>
}

@RenderBody()